# Example environment for Service Bus Inspector UI
# Do NOT commit real secrets. Create a local .env.local with your values.

VITE_API_BASE_URL=http://localhost:7071
# Optional local testing only (never commit real values)
# VITE_REACT_APP_TEST_CONNSTR=Endpoint=sb://<ns>.servicebus.windows.net/;SharedAccessKeyName=RootManageSharedAccessKey;SharedAccessKey=***
# VITE_KEYVAULT_NAME=kv-your-dev
